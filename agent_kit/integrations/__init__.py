"""
Adapters that report other agent harnesses to agent-kit Cloud.

- ``agent_kit.integrations.claude_agent_sdk`` — Claude Agent SDK (``pip install agent-kit-ai[claude-agent-sdk]``)
- ``agent_kit.integrations.openai_agents`` — OpenAI Agents SDK (``pip install agent-kit-ai[openai-agents]``)

Nothing is imported here, so installing agent-kit never requires either harness.
"""
